## Problem

What can't you do today? Describe the use case, not the solution.

## Proposed behavior

What should happen? If it affects enforcement or the manifest format, show:

```yaml
# cube YAML sketch for the proposed behavior
```

```bash
fsb run --cube <name> -- <command...>
```

## Environment

- `fsb --version`: <!-- output of `fsb --version` -->
- Platform: <!-- e.g. macOS 15 (arm64) / Ubuntu 24.04 (x64) / Windows 11 -->
