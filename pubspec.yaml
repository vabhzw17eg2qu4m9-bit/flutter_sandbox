name: flutter_sandbox
description: Standalone command sandboxing - run any console command inside a policy or kernel sandbox (macOS sandbox-exec, Linux unshare, Windows Job Object descriptor).
version: 0.1.3
repository: https://github.com/vabhzw17eg2qu4m9-bit/flutter_sandbox
environment:
  sdk: '>=3.12.0 <4.0.0'
dependencies:
  crypto: ^3.0.3
  yaml: ^3.1.3
dev_dependencies:
  coverage: ^1.15.1
  lints: ^6.1.0
  test: ^1.31.2
